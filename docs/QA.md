# QA visual-итерации главной

## Текущая итерация — 29 августа 2026

### Правки по разбору дизайнера

- По сообщениям и семи снимкам из Telegram-экспорта `ChatExport_2026-08-29` уточнены пять зон главной: CTA статей, действия блока «Будем знакомы», внутренний ритм услуг, карточка запроса о форме тела и диагональные стрелки.
- CTA «Читать» выровнен по правому краю карточек блога. Ссылка «Образование и сертификаты» стала вторым крупным, но спокойным outline-действием рядом с «Больше обо мне».
- В карточках «Формула тела» и «Наставничество» действия и примечания собраны в одинаковые группы; совпадают интервалы и нижние отступы. На 320, 390, 768 и 1440 px горизонтального переполнения нет.
- «Хочу изменить форму» получает управляемый перенос после слова «Хочу». На 320 px все четыре карточки имеют одинаковую высоту, а заголовки и описания внутри каждой строки выровнены.
- Все интерфейсные Unicode-глифы (`↗`, `→`, `←`, `↓`, `✓`, `▶`, `×` и FAQ-плюс) заменены точными SVG-ассетами Coolicons из переданного Figma-файла. В репозитории сохранены восемь исходных экспортов; общий компонент выводит их как цветовые CSS-маски, скрытые от accessibility tree.
- Browser QA выполнен для 22 маршрутов на 390 и 1440 px: 44 проверки не нашли оставшихся emoji-глифов, невидимых SVG-масок, битых изображений или горизонтального переполнения. Отдельно проверены вращение FAQ-плюса, открытые бургер-меню и lead-popup, play/check/back/down/CTA-иконки и псевдоэлемент списка.

### Маршруты GitHub Pages

- Исправлена нормализация `window.location.pathname` в общем entrypoint внутренних страниц: перед поиском страницы удаляется `import.meta.env.BASE_URL`. Раньше физический HTML отдавался с кодом 200, но пути вида `/svetlana-ilina/o-svetlane/` не совпадали с реестром `/o-svetlane/`, поэтому интерфейс показывал внутренний экран 404.
- В локальном preview с base `/svetlana-ilina/` проверены все 22 маршрута. На каждой странице есть ожидаемый H1 и один `main`; внутренних экранов «Страница не найдена», ссылок мимо base, незагруженных изображений и отсутствующих маршрутов нет.

### Подготовка к GitHub Pages

- Пользователь выбрал красное фото для Hero; `activeHeroPortrait = 'red'`. Чёрные WebP и настройка переключения сохранены.
- Перед коммитом выполнен `git fetch github`: локальная `main` совпадала с `github/main` (`f32a857`), конфликтов с удалёнными изменениями нет.
- Адрес существующего GitHub Pages подтверждён через статус предыдущего успешного deployment: `https://mystaxe.github.io/svetlana-ilina/`. В режиме `github-pages` задан соответствующий `SITE_PUBLIC_ORIGIN`; проверены абсолютные OG/X URL.
- Повторно прошли `npm.cmd run build:github-pages` и `npm.cmd run build`: TypeScript, 22 маршрута, метаданные, публичные assets и VTT. Локальный preview возвращён к base `/`.
- В составе изменений нет исходных MOV, лабораторных PNG, технических расшифровок или локальных снимков QA. Выборочные текстовые показатели остаются без личных реквизитов; исходные файлы с личными данными в Git не добавляются.

### Попап заявки и отступы перед футером

- В общий `PageShell` добавлен `LeadDialog` с существующей формой. Кнопки с `/formula-tela/#request` и `#request` открывают её на текущей странице; информационные ссылки не перехватываются. Форма на странице услуги сохранена как запасной вариант для прямой ссылки. Поля двух экземпляров имеют уникальные ID.
- Нативный `dialog` удерживает фокус и делает фон неактивным. Панель и затемнение анимируются в обе стороны (320/240 мс); прерывание открытия продолжает движение из текущего кадра. Прокрутка остаётся заблокированной до конца закрытия, затем фокус возвращается на кнопку без изменения позиции страницы.
- В браузере прошли открытие кнопкой и Enter, крестик, Escape, клик снаружи, Tab/Shift+Tab, повторное открытие с сохранением полей и открытие после мобильного меню. Проверены все шесть историй, услуга и контакты: маршрут не меняется, дубли ID отсутствуют.
- В готовой сборке проверены 320×568, 390×844, 768×1024, 1024×768 и 1440×960: панель помещается в экран, горизонтального переполнения нет, крестик остаётся доступным. Длинная форма прокручивается внутри окна. Проверены обязательные поля и согласие.
- Ответ формы занимает заранее выделенное место. При демонстрационной отправке высота мобильной панели и позиция страницы не меняются; при необходимости плавно прокручивается только содержимое окна. Черновик хранится лишь в текущей странице, без браузерного хранилища.
- Серверная отправка по-прежнему не подключена: форма явно сообщает, что заявка не отправлена. Медицинские документы и подробности не запрашиваются.
- У завершающего `StoryCTA` убран случайный нижний margin; отступ до футера задаёт общий контейнер историй: 72–96 px. Верхний ритм сохранён: например, у Даши до акцентного блока уже было 72+24 px, а после — лишь 24 px.
- `prefers-reduced-motion` отключает переход попапа и плавную прокрутку ответа; смена настройки завершает текущую анимацию. Эти ветви проверены по коду, без эмуляции системной настройки.
- `npm.cmd run build` прошёл: TypeScript и проверка 22 маршрутов, metadata и captions. В production-preview ошибок и предупреждений консоли не обнаружено. Снимки и локальный протокол: `../output/lead-popup-2026-08-29/` относительно корня репозитория.

## Итерация — 28 августа 2026

### Фото услуги и раскрывающиеся элементы

- `.service-card__image` показывает фотографию целиком через `object-fit: contain`. В браузере проверены 320, 375, 390, 767, 768, 1023, 1024, 1279, 1280, 1440, 1920 и 2560 px: изображение внутри контейнера, горизонтального переполнения нет. На мобильном увеличена высота кадра и уточнён `sizes`.
- Общий контроллер `details/summary` анимирует высоту и прозрачность в обе стороны (280/240 мс), разворачивает незавершённый переход от текущего кадра и освобождает фиксированную высоту после завершения/resize. Нативная разметка сохранена; Enter и пробел используют тот же переход.
- В production-preview проверены все 18 раскрытий: 5 FAQ главной, 6 FAQ услуги, 4 подписи видео Даши, 2 подписи видео Веры и аудиорасшифровка Оксаны. Открытие, закрытие, `aria-expanded`, скрытие содержимого через `inert` и возврат исходной высоты прошли. Быстрые повторные нажатия не оставляют зависший `open` или обрезанный контент.
- У FAQ убраны внешние прямые разделители. Каждый пункт имеет собственную цельную скруглённую рамку; значок поворачивается и при открытии, и при закрытии.
- Меню проверено на 320, 390, 768, 1024 и 1279 px: измерены промежуточные позиции панели, фон и прокрутка остаются заблокированы до конца закрытия, фокус возвращается без скачка. Прошли Escape, циклический Tab/Shift+Tab, клик по фону, переходы на страницу и к якорю, сброс при переходе на desktop breakpoint.
- `prefers-reduced-motion` предусмотрен в обоих контроллерах, включая изменение настройки во время анимации; CSS отключает анимацию значков. Эти ветви проверены по коду: эмуляция системной настройки в доступном браузере не проводилась.
- `npm.cmd run check` и `npm.cmd run build` проходят. Браузерные ошибки на проверенных страницах не обнаружены. Локальные протоколы раскрытий и переходов меню: `../.dbg/motion-2026-08-28/` относительно корня репозитория.

### Проба цвета Hero

Пользователь попросил отдельный вариант с красным боди, сохранив чёрный. Встроенный ImageGen отклонил обработку фильтром безопасности; нового изображения нет. После этого пользователь согласовал сравнение с ранее присланным готовым красным фото: в Hero подключён `svetlana-hero-red.webp` (851 × 1280, светлое кресло). Чёрный оригинал и три WebP не менялись; для возврата достаточно поменять `activeHeroPortrait` на `black` в `src/data/home.ts`. Это переключение между двумя пользовательскими фотографиями, а не новая перекраска. В alt используется название одежды «боди».

Красный вариант проверен на 320, 390, 768, 1024 и 1440 px: файл загружается, пропорции сохранены, горизонтального переполнения нет; на мобильном лицо расположено ниже кнопок. Сборка `npm.cmd run build` и проверка 22 маршрутов прошли, консоль без ошибок. Снимки и замеры сохранены локально в `../.dbg/hero-color-2026-08-28/` относительно корня репозитория.

Дополнение после материалов Веры: 18 проверок трёх обновлённых маршрутов на 320–1440 px прошли, два новых видео воспроизводятся и взаимно останавливаются. Проверены фото «до» со встроенным стикером и новый `srcset` качественного портрета для hero. TypeScript и `build:sites` проходят; публичных копий лабораторных бланков и их личных реквизитов нет.

Сборки `check`, `build`, `build:sites` и `build:github-pages` проходят. Проверяются 22 маршрута, ссылки на assets, OG/X-метаданные и структура субтитров. После проверки GitHub Pages восстановлен обычный base `/`.

В production-preview выполнены 87 проверок страниц: 22 маршрута на 1440, 390 и 320 px и семь основных на 375, 768 и 1024 px. Проверены переполнение, H1/main, дубли ID, внутренние якоря и загруженные изображения. Все 12 фильтров архива работают. Четыре видео Даши запускаются; субтитры отображаются; следующий ролик останавливает предыдущий. Тест пройден с проверкой возврата, валидации и демонстрационного результата.

На мобильном исправлено перекрытие лица в hero; длинный заголовок политики больше не расширяет страницу. Детали и ограничения проверки — в [отчёте о редизайне](REDESIGN_2026-08-28.md). WordPress runtime и внешняя публикация не проверялись, ручная проверка всех автосубтитров на слух не выполнена.

Ниже сохранены исторические отчёты. Их сведения о шрифтах, запрете градиентов, количестве маршрутов и структуре главной относятся к предыдущему дизайну.

Дата проверки: 18 июля 2026.

## Автоматические проверки

- `npm.cmd run check` — TypeScript без ошибок.
- `npm.cmd run build` — production multi-page build успешен.
- Статический поиск в `src` и `public` — 0 запрещенных palette utilities, prototype-blue, gradients, glass, shadows, inline styles и `!important`.
- В quiz/adapters нет `fetch`, XHR, beacon, WebSocket, local/session storage или WordPress endpoint.
- Reducer, scoring, validation, quiz data и adapters не изменены; motion-итерация затронула только представление экранов и отказ от автоматического переноса фокуса при первой загрузке теста.

## Browser QA главной

Проверена реальная Vite-страница на шести ширинах:

| Width | Horizontal overflow | Targets < 44 px | Header mode | H1 |
|---:|---|---:|---|---:|
| 320 | нет | 0 | mobile menu | 1 |
| 375 | нет | 0 | mobile menu | 1 |
| 768 | нет | 0 | mobile menu | 1 |
| 1024 | нет | 0 | mobile menu | 1 |
| 1280 | нет | 0 | desktop nav | 1 |
| 1440 | нет | 0 | desktop nav | 1 |

Дополнительно:

- Desktop Hero занимает 0.90 / 0.90 / 0.93 viewport height на 1024 / 1280 / 1440 px.
- Computed H1-size: 40 px на 320, 40.025 px на 375, 58.496 px на 768, 70.528 px на 1024, 82.56 px на 1280, 90.08 px на 1440.
- Browser загрузил `Source Serif 4` для H1/H2 и `Manrope Variable` для body и прикладных ролей; `document.fonts.status = loaded`.
- На странице 12 semantic content sections, один H1, header/main/footer landmarks.
- Mobile menu: `aria-expanded`, scroll lock, dialog, Escape, возврат фокуса. DOM tab order идет от close control через navigation к CTA; focus trap ограничен panel.
- FAQ: нативный `details/summary`, высота summary 80 px, red plus и open-rotation 45°.
- Console на всех проверенных маршрутах — без warning/error.

## Responsive images

На главной семь `<picture>` instances. Для каждого `<img>` заданы `alt`, `width`, `height`, `sizes`, `loading`, `fetchpriority` и fallback. Hero загружается eager/high, остальные — lazy/auto. AVIF/WebP `<source>` добавятся только при появлении реальных `srcset`, поэтому прототип не запрашивает несуществующие файлы.

## Smoke-check соседних маршрутов

`/formula-tela/` и `/test/` проверены на 375 и 1280 px:

- overflow отсутствует;
- один H1, header и footer присутствуют;
- mobile/desktop navigation переключается на том же breakpoint;
- computed brand surface — `rgb(193, 18, 31)`;
- browser console чиста.

## Исправления по результатам QA

- Устранен overflow на 1024 px у крупного «8–10 недель»: aside расширен до пяти grid columns.
- Короткие header/footer links получили минимальную ширину 44 px.
- Значимый текст на красных surfaces переведен в непрозрачный белый для AA-контраста.
- Номера 01–05 на black surface увеличены до 24 px при сохранении красного акцента.
- Уточнены responsive `sizes` для hero, feature images и двух типов blog articles.

## Motion-итерация

Дата автоматической проверки: 24 июля 2026.

- `npm.cmd run check` и `npm.cmd run build` проходят.
- Длительности унифицированы до 180/220/260 мс; `transition: all`, inline styles и `!important` не добавлены.
- Reveal наблюдает 41 независимую смысловую группу через один `IntersectionObserver`: секция и её фон остаются стабильными, а непосредственные элементы входят каскадом 0/40/80/120/150 мс.
- `prefers-reduced-motion` отключает smooth scroll, stagger, page/quiz animations и задержки.
- Mobile menu закрывается после exit-перехода, блокирует фон через `inert`, сохраняет Escape/focus trap и корректно обрабатывает same-page anchors.
- DOM/browser QA выполнен на 320, 768 и 1280 px: горизонтального overflow нет, скрытых motion-элементов в viewport после завершения каскада нет, все 118 элементов принадлежат группам, в консоли нет ошибок.

## «Истории перемен»

Дата автоматической проверки: 29 июля 2026.

- `npm.cmd run check` — TypeScript без ошибок.
- `npm.cmd run build` — успешна production-сборка пяти маршрутов, включая `/istorii-peremen/` и `/istorii-peremen/polina/`.
- MU-plugin регистрации CPT проверен PHP 8.2: синтаксических ошибок нет.
- В production-выводе присутствуют оба HTML-маршрута, Worker и hosting metadata.
- В публичном исходном тексте нет старого `/cases/`, слова «кейс» и неподтвержденной цифры `−13`.
- CTA не использует формулировку «Записаться на диагностику»; услуга названа экспертным разбором.
- Статический поиск не нашел gradients, glass/backdrop blur, тяжелых shadow utilities, `transition-all` и `!important`.
- Архив использует `button` с `aria-pressed`, touch target 52 px, live-status и синхронизацию выбранной проблемы с URL.
- На архиве и в истории Полины по одному H1; 12 экранов истории имеют последовательные H2 и связаны через `aria-labelledby`.
- Видео Полины отдаются в совместимом MP4, имеют постеры, подписи, нативные элементы управления и не запускаются автоматически.
- Для отсутствующих фото и видео используются неинтерактивные заглушки: ложных play-кнопок и неподтвержденной цифры результата нет.

Browser QA этой итерации не выполнялся: изменения проверены структурно и production-сборкой.

## Архивная проверка внешнего WordPress-прототипа

Дата автоматической проверки: 29 июля 2026.

Ниже сохранён исторический отчёт по внешнему каталогу `app/public/wp-content`, которого нет в текущем репозитории. Эти пункты не подтверждают готовность нынешней версии к редактированию из админки WordPress.

- `npm.cmd run check` и `npm.cmd run build` проходят.
- Production-сборка содержит 17 HTML-маршрутов: основной sitemap, историю Полины и три страницы статей.
- Все пути из header, footer, главных CTA и карточек блога имеют соответствующий HTML entry или якорь существующего маршрута.
- `svetlana-required-pages.php` и `svetlana-transformation-stories.php` проходят PHP 8.2 lint.
- WordPress bootstrap не удаляет пример-страницу и не перезаписывает существующие страницы, статусы или тексты.
- Legal-страницы и статьи создаются черновиками; публичные структурные страницы создаются опубликованными.
- Главная и страница записей назначаются автоматически только при пустых `page_on_front` и `page_for_posts`.
- История Полины создаётся черновиком; исходные problem-термины назначаются только при её первом создании.

WordPress runtime-smoke не выполнялся, потому что локальный веб-сервис WordPress не был запущен; PHP-синтаксис и условия идемпотентности проверены по коду.
