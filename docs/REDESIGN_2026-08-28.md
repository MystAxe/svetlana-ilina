# Редизайн и новые истории — 28 августа 2026

## Результат итерации

Работа выполнена поверх `f32a857`: перед изменениями получены четыре новых коммита из `github/main` без конфликтов. Новые фотографии Светланы, история Оксаны и восемь документов об образовании сохранены. 29 августа пользователь выбрал красный Hero и поручил отправить накопленные изменения в существующий репозиторий GitHub; публикация сайта настроена его workflow GitHub Pages.

Основной ориентир — первый референс пользователя, Moonsync. Сохранён красный акцент; чужие фотографии, тексты, логотипы и показатели из референсов не используются.

- Главная: широкий фотографический hero, короткий оффер, четыре входа по запросам, подход, три истории, услуги, знакомство со Светланой, тест, блог и FAQ.
- Общий стиль: Manrope вместо книжных заголовков, спокойные нейтральные поверхности, округлые кнопки и панели, больше реальных фото. Нумерация «глав» убрана из историй.
- Перестроены «Формула тела», архив историй и общие шаблоны. Существующие истории, тест, наставничество, профиль, блог и служебные страницы используют общие стили. Всего в сборке 22 маршрута.
- Добавлены истории Даши и Веры; в архиве шесть историй и фильтры по запросам, включая тягу к сладкому и форму тела.
- Название подхода унифицировано как «Четыре направления». CTA услуги в историях — «Записаться на разбор». Изображения-заглушки блога заменены текстовыми карточками.

## Hero: что сделано и что не сделано

Для Hero сохранены две переданные пользователем фотографии: красное боди со светлым креслом (`svetlana-hero-red.webp`, 851 × 1280) и качественный `IMG_3483.JPG` (2784 × 4176), где боди чёрное, а кресло синее. Из качественного оригинала созданы `svetlana-hero-studio-640.webp`, `-960.webp`, `-1600.webp` (28/53/143 КБ) с адаптивным `srcset`. После сравнения 29 августа пользователь выбрал готовое красное фото для текущей версии сайта. Чёрные файлы и настройки сохранены: возврат — `activeHeroPortrait = 'black'` в `src/data/home.ts`. Цвета одежды и кресел не менялись.

Попытка улучшить первое фото и достроить широкий фон через ImageGen завершилась отказом автоматического фильтра. Позднейшая отдельная просьба перекрасить чёрное боди также не дала результата в ImageGen; после отказа пользователь согласовал уже имеющееся красное фото. В Hero фон продолжен средствами CSS, пропорции человека сохранены. На мобильном экране текст и кнопки находятся выше лица. Ретушь лица, света, новая перекраска и генеративное расширение фона не выполнены.

## История Даши

Маршрут: `/istorii-peremen/dasha/`. Сюжет — отношения со сладким, энергия, питание и тренировки, без выдуманной потери веса и универсальных обещаний результата.

Материалы из отдельного сообщения пользователя сопоставлены по его подписям:

| Исходник | Публичный материал | Обработка |
|---|---|---|
| `IMG_2721.MOV` | `dasha-diet.mp4` | Убраны первые 4,1 с с вопросом интервьюера; итог 11,933 с |
| `IMG_2724.MOV` | `dasha-changes.mp4` | Рассказ о переменах, 21,337 с |
| `IMG_2725.MOV` | `dasha-chocolate.mp4` | Фрагмент с шоколадкой, 19,875 с |
| `IMG_2708.MOV` | `dasha-training.mp4` | Тренировка, 14,133 с; звуковая дорожка удалена |
| `IMG_2703.JPG` | `dasha-meals.webp` | Реальный коллаж питания после изменений, 1200 × 1500 |
| `IMG_2722.PNG`, `IMG_2723.PNG` | Три текстовые карточки показателей | Исходные скриншоты не добавлены в публичные файлы |

Все публичные материалы находятся в `public/media/stories/dasha/`. Для видео подготовлены постеры; используется MP4/H.264, 720 × 1280, yuv420p и AAC для разговорных фрагментов. Метаданные удалены, включён faststart. До действия посетителя видео не загружаются целиком и не воспроизводятся. Крупная кнопка запуска доступна с клавиатуры; нативные элементы управления сохранены. При запуске следующего ролика предыдущее видео/аудио на странице останавливается.

Две фотографии прежнего питания взяты из Telegram-экспорта: `photo_12464@28-08-2026_17-54-46.jpg` и `photo_12468@28-08-2026_17-54-46.jpg`. Их публичные версии — `dasha-before-breakfast.webp` и `dasha-before-snack.webp`. Подписи не объявляют отдельные продукты запрещёнными.

### Расшифровка и субтитры

Речь распознана локально через faster-whisper; записи не отправлялись внешнему сервису распознавания. Для трёх разговорных фрагментов добавлены `.vtt` с меткой «Русские (авто)» и краткие текстовые описания. Неуверенная фраза в ролике с шоколадкой на 6,52–8,08 с обозначена `[неразборчиво]`. Полная ручная проверка субтитров на слух не выполнена и остаётся перед публикацией. Не считать автоматическую расшифровку утверждённой стенограммой.

Локальный скрипт подготовки медиа находится вне репозитория: `../.tools/prepare-dasha-media.ps1`. Исходные MOV, скриншоты, модели распознавания и технические расшифровки в Git не добавлены.

### Анализы и приватность

Показаны только выбранные значения из предоставленного бланка: железо 4,1 мкмоль/л, ферритин 6 мкг/л, витамин D 14,3 нг/мл. Указана принадлежность исходному бланку и добавлено пояснение о границах интерпретации. Нет полного имени, номера исследования, QR-кодов, скриншотов или PDF. Нет утверждения о доказанной причине тяги к сладкому, лечении или улучшении повторных анализов. Перед внешней публикацией нужно подтвердить разрешённый объём использования этой истории и показателей.

## История Веры

Маршрут: `/istorii-peremen/vera/`. Использован отдельный сценарий из переписки, не история Вики. На первом экране не выводятся имя и возраст. Текст посвящён форме тела, питанию, подходящей нагрузке и наблюдению за изменениями.

В сообщении 20:26 получен точно подписанный комплект. На фото и видео уже есть встроенное закрытие лица; оно сохранено в итоговых файлах и постерах. Материалы других клиенток не использованы.

| Исходник | Веб-версия и расположение |
|---|---|
| `IMG_2711.JPEG` | `vera-before-640.webp`, `vera-before-1200.webp` — фото в начале работы, не иллюстрация результата |
| `IMG_2718.MOV` | `vera-training.mp4` и постер — 14,8 с тренировочного процесса, на первом экране и в обложке архива |
| `IMG_2720.MOV` | `vera-scales.mp4` и постер — 7,467 с, один замер в блоке наблюдений |
| `IMG_2726.PNG`, `IMG_2727.PNG`, `IMG_2728.PNG` | Только выбранные значения; скриншоты в публичную папку не копировались |

Подготовка: `../.tools/prepare-vera-media.ps1`. Оба ролика — MP4/H.264, 720 × 1280, yuv420p, faststart, без звука и исходных метаданных. Исходники не изменены. Для проверки приватности просмотрены контактные листы с шагом 0,5 с и отдельные крупные кадры; постеры взяты из кадров с закрытым лицом. Аудио дополнительно проверено локальным распознаванием: надёжная содержательная расшифровка не получена, реплики не выдуманы; веб-версии обозначены как видео без звука.

В карточках показаны ферритин 66 мкг/л, витамин D 25,5 нг/мл и свободный Т4 15,51 пмоль/л. Для последних двух сохранено указание на отметку лаборатории. Указано, что это выборка, в бланках есть и другие отметки; нет вывода «всё в норме», диагноза или назначений. ФИО, дата рождения, номер исследования, QR-коды и оригинальные документы не размещены в публичных assets. Из одного видео взвешивания не выводится потеря веса; фото «после» не подменяется фотографией «до».

Медиа подготовлены для локального прототипа. С учётом возраста героини из переписки необходимо отдельно подтвердить допустимый объём внешней публикации истории, показателей и медиа с ней и её представителем. Скрытие лица не заменяет согласование.

## Основные файлы

| Область | Файлы |
|---|---|
| Общий стиль | `src/styles/app.css`, `src/styles/redesign.css` |
| Главная | `src/components/home/HomeHero.ts`, `HomeSections.ts`, `src/pages/homePage.ts` |
| Истории | `src/components/stories/StoryCard.ts`, `StoryVideo.ts`, `src/lib/storyMedia.ts`, `src/data/stories.ts` |
| Новые страницы | `src/data/dashaStory.ts`, `veraStory.ts`, соответствующие `src/pages`, `src/entries` и HTML-маршруты |
| Услуга и общие страницы | `src/pages/formulaPage.ts`, `staticPage.ts`, `src/components/sections/Hero.ts` |
| Метаданные и сборка | `config/socialMetadata.ts`, `vite.config.ts`, `scripts/site-entries.mjs`, `scripts/verify-static-build.mjs` |

Старые визуальные компоненты, которые больше не используются на главной, не удалены без необходимости. Неиспользуемый Source Serif сохранён для совместимости со вспомогательным WordPress-скриптом, но не применяется к новому интерфейсу.

## Метаданные и упаковка

Для общих страниц создана и визуально проверена типографическая social-карточка `public/og.png`. Карточки отдельных историй используют собственные изображения; для Веры теперь подключён постер её тренировки с закрытым лицом. Статьи без собственного изображения не наследуют общую картинку. На всех маршрутах задаются соответствующие странице OG/X-заголовок и описание.

Абсолютные URL метаданных появляются только при заданном `SITE_PUBLIC_ORIGIN`. Production-домен не придуман. Сборка с доверенным локальным origin проверена отдельно; финальная локальная сборка имеет обычный base `/`. Проверка сборки контролирует маршруты, assets, метаданные и структуру VTT.

Привязанный проект Sites при проверке вернул `project_not_found`. Новый проект не создавался, привязка не изменялась, публикация в Sites не выполнялась. Локальная упаковка `build:sites` работает. 29 августа для текущей версии выбран существующий GitHub; подробности — в `DEPLOYMENT.md`.

## Проверки

- Успешны `npm.cmd run check`, `npm.cmd run build`, `npm.cmd run build:sites`, `npm.cmd run build:github-pages`.
- Для GitHub Pages проверен base `/svetlana-ilina/`; затем восстановлена сборка с base `/`.
- В production-preview выполнены 87 проверок страниц: все 22 маршрута при 1440, 390 и 320 px; семь основных дополнительно при 375, 768 и 1024 px. Нет горизонтального переполнения, дублирующихся ID, отсутствующих внутренних якорей и сломанных загруженных изображений; на страницах по одному H1 и main.
- Визуально просмотрены главная и основные внутренние страницы. Исправлены перекрытие лица кнопками в мобильном hero, кадрирование фото «Формулы тела» и переполнение длинного юридического заголовка. После последнего изменения кадра повторно выполнена сборка и проверена услуга на 1440 и 390 px.
- Проверены все 12 фильтров архива, обновление URL, мобильная навигация и переход к подходу с другого маршрута.
- Все четыре видео Даши запущены в браузере, подтверждён показ субтитров разговорного фрагмента и остановка предыдущего ролика при переключении.
- Тест пройден на мобильном экране: блокировка перехода без ответа, сохранение выбора при возврате, валидация контактов и демонстрационный результат. Использованы только вымышленные тестовые данные; отправка на сервер не подключена.
- При быстрых навигациях в dev встречался `AbortError: Transition was skipped`; чистота консоли во всех возможных сценариях не заявляется. WordPress runtime и внешний deploy не проверялись.

После получения комплекта Веры и качественного hero повторно прошли TypeScript, `build:sites` и `build:github-pages`; финальная сборка возвращена к base `/`. Дополнительно проверены главная, Вера и архив на шести ширинах 320/375/390/768/1024/1440 px: 18 проверок, без переполнения, дублирующихся ID, сломанных загруженных изображений и нарушений количества H1/main. Оба новых видео запущены в production-preview; следующий ролик останавливает предыдущий. Сохранность исходных стикеров проверена на странице. Проверка текста и списка публичных файлов не обнаружила реквизитов лабораторных бланков.

## Что ещё нужно перед публикацией

1. Подтвердить допустимый объём внешней публикации материалов Веры; файлы со встроенным закрытием лица уже получены и подключены.
2. Проверить автосубтитры на слух и утвердить публичный объём истории Даши, включая выбранные показатели.
3. При необходимости отдельно выполнить ретушь/расширение исходного hero-фото — текущая версия использует CSS-композицию с оригиналом.
4. Подтвердить цены, длительность сопровождения, контакты и юридические тексты; подключить реальный обработчик заявок. Тест и формы пока демонстрационные.
5. Перед окончательным запуском согласовать собственный домен, если он нужен. Для текущего прототипа выбрана существующая публикация GitHub Pages.
